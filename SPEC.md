# กระสุนคือคำตอบ (Tank-Quiz) — Implementation Spec v0.1

> เอกสารนี้เขียนสำหรับ **coding agent** ไม่ใช่สำหรับอ่านเอาแรงบันดาลใจ
> ทุกตัวเลขคือค่าเริ่มต้นที่ implement ได้ทันที ทุก section ที่ขึ้นต้นด้วย `MUST` คือข้อบังคับ
> ถ้าเจอจุดที่ spec ขัดกันเอง ให้ยึด §2 (Design invariants) เป็นใหญ่ที่สุด แล้วรายงานกลับ

---

## 0. TL;DR สำหรับ agent

สร้างเกมรถถัง 2D pixel แนว Battle City แบบ multiplayer ในห้องเดียวกัน (co-located party game):

- **จอใหญ่ 1 จอ** (โปรเจกเตอร์/ทีวี) = สนามรบ
- **มือถือ N เครื่อง** = คนตอบคำถาม
- แต่ละทีมมี **คนขับ 1 คน** (คีย์บอร์ด/เกมแพด ที่จอใหญ่ หรือ virtual stick บนมือถือ) + **คนตอบไม่จำกัดจำนวน**
- ตอบ quiz ถูก → ได้กระสุน → คนขับเอาไปยิง
- **ทีมสุดท้ายที่รอด ชนะ**

**สิ่งที่ต้องสร้างก่อนอย่างอื่น:** รถถังเดินบนกริดและยิงกำแพงอิฐให้พังได้ พร้อม juice ครบ (§9)
ถ้ายังไม่สนุกตอนไม่มี quiz ห้ามเดินต่อไป milestone ถัดไป (ดู §11 M3 GATE)

---

## 1. Stack ที่แนะนำ

| ส่วน | เลือกใช้ | เหตุผล |
|---|---|---|
| Server | Node.js 20+ · `ws` | authoritative sim, ไม่ต้องมี build step |
| Host screen | HTML5 Canvas 2D | pixel art ง่ายสุด, `imageSmoothingEnabled = false` |
| Phone client | HTML + vanilla JS | ห้ามให้ผู้เล่นติดตั้งอะไร เข้าผ่าน URL + room code |
| Transport | WebSocket (JSON) | ปริมาณข้อมูลน้อยมาก ไม่ต้อง binary ใน MVP |
| State | in-memory เท่านั้น | ไม่มี DB ใน MVP · question bank เป็นไฟล์ JSON |

**MUST:** server เป็น authoritative ทั้งหมด client ไม่คำนวณ collision/damage เอง
**MUST:** ไม่มี account, ไม่มี login — join ด้วย room code 4 ตัวอักษร (A–Z ไม่มี I, O) + ใส่ชื่อเล่น
**MUST NOT:** ห้ามใช้ framework ใหญ่ (React/Unity/Phaser) ใน MVP — เป้าหมายคือ iterate เร็ว

---

## 2. Design invariants (ห้ามละเมิด)

กฎ 6 ข้อนี้คือเหตุผลที่เกมนี้สนุก ถ้า implement แล้วข้อไหนหาย = สร้างผิดเกม

| ID | Invariant | ผลถ้าละเมิด |
|---|---|---|
| **INV-1** | **สนามไม่หยุดตอนขึ้นคำถาม** รถถังยังวิ่ง ยังยิง ยังตายได้ ตลอดหน้าต่างตอบ | เกมกลายเป็นควิซที่มีภาพประกอบ ความกดดันหายหมด |
| **INV-2** | **คนขับเห็นแค่ 8 ช่องรอบตัว แต่คนตอบเห็นแผนที่ทั้งใบ** | ไม่มีเหตุผลให้คนตอบมองจอ ห้องจะเงียบ |
| **INV-3** | **กระสุนทุกนัดมีชื่อเจ้าของ** และ kill feed ต้องประกาศชื่อคนตอบ | เสียง 1 ใน N ไม่มีความหมาย คนเลิกตั้งใจตอบ |
| **INV-4** | **กระสุนต้องขาดแคลน** คนขับต้องเคยอยู่ในสภาพ "ไม่มีกระสุน" อย่างน้อย 3 ครั้ง/แมตช์ | การตอบถูกไม่รู้สึกเป็นรางวัล |
| **INV-5** | **ไม่โชว์คำตอบรายคนต่อสาธารณะ** จอใหญ่โชว์แค่ผลรวม ฟีดแบ็กรายคนขึ้นบนมือถือตัวเองเท่านั้น | คนโดนประจาน แล้วเลิกกล้าตอบ |
| **INV-6** | **แผนที่ต้องพังได้** กำแพงอิฐเปลี่ยนรูประหว่างเล่น | สนามนาทีที่ 6 หน้าตาเหมือนนาทีแรก = น่าเบื่อ |

---

## 3. ค่าคงที่ทั้งหมด (`constants.js`)

หน่วยโลก: **1 cell = 16 world units (wu)** · แผนที่ 32×32 cells = 512×512 wu

```js
export const C = {
  // --- world ---
  GRID: 32,              // cells ต่อด้าน
  CELL: 16,              // wu ต่อ cell
  SNAP: 8,               // wu — รถถัง snap แกนตั้งฉากทุก 8 wu (ฟีล Battle City)
  TICK_HZ: 30,           // fixed timestep ของ server
  SNAPSHOT_HZ: 20,       // ความถี่ส่ง state ให้ host screen

  // --- tank ---
  TANK_SIZE: 32,         // wu (2x2 cells)
  TANK_HP: 3,
  TANK_SPEED: 48,        // wu/s  = 3 cells/s
  TURRET_DIRS: 8,        // ป้อมปืนหมุนแยกจากตัวถัง
  DASH_CD: 4.0,          // s
  DASH_DIST: 48,         // wu
  DASH_NO_FIRE: 1.0,     // s ยิงไม่ได้หลัง dash
  RAM_DMG: 0.5,          // ดาเมจที่ให้ศัตรู
  RAM_SELF: 0.5,         // ดาเมจที่ตัวเองรับ
  RAM_CD: 1.5,           // s

  // --- bullet ---
  BULLET_SPEED: 192,     // wu/s = 12 cells/s
  FIRE_CD: 0.6,          // s
  RICOCHET_STEEL: 1,     // สะท้อนกำแพงเหล็กได้ 1 ครั้ง

  // --- vision (INV-2) ---
  FOV_RADIUS: 8,         // cells — สำหรับคนขับเท่านั้น
  PING_MAX: 3,           // ต่อคน ต่อแมตช์
  PING_CD: 5.0,          // s
  PING_SHOW: 3.0,        // s ที่ปิงค้างบน HUD คนขับ

  // --- quiz ---
  ANSWER_WINDOW: [12, 10, 7],  // s ตาม round 1/2/3
  GAP_BETWEEN_Q: 3.0,          // s (สนามยังเดิน — INV-1)
  AMMO_CAP: [6, 4, 3],         // ตาม round
  CALLED_SHOT_EVERY: 3,        // ทุกข้อที่ 3 = ยิงชี้เป้า (เริ่ม round 2)
  CONSENSUS_DIVISOR: 1.6,      // ดู §5

  // --- match ---
  ROUND_ENDS: [120, 270, 360], // s: R1 จบ 2:00, R2 จบ 4:30, R3 จบ 6:00
  STORM_RINGS: [                // เริ่มบีบเมื่อไร บีบเหลือกี่ cell
    { at: 120, inset: 5 },
    { at: 270, inset: 8 },
  ],
  STORM_DPS: 0.5,              // HP/s เมื่ออยู่นอกวง
  KILLCAM_SEC: 1.5,
};
```

### ตารางอ้างอิงสำหรับมนุษย์

| ค่า | ตั้งต้น | เหตุผล |
|---|---|---|
| ขนาดแผนที่ | 32 × 32 cells | ใหญ่พอให้ซ่อน เล็กพอให้เจอกันใน 2 นาที |
| HP รถถัง | 3 | ตายได้แต่ไม่ตายฟรี ทำให้กระสุนด้าน (0.5 dmg) มีความหมาย |
| ความเร็ววิ่ง | 3 cells/s | ช้าพอให้เสียงเตือนจากเพื่อนมาทัน |
| ความเร็วกระสุน | 12 cells/s | เร็ว 4 เท่าตัวรถ = หลบได้ถ้าเห็นก่อน |
| คลังกระสุน | 6 → 4 → 3 | บีบตามยก ทุกนัดมีค่าขึ้นเรื่อย ๆ (INV-4) |
| รัศมีมองเห็น | 8 cells | ต่ำกว่านี้อึดอัด สูงกว่านี้ไม่ต้องพึ่งเพื่อน |
| กำแพงอิฐ | 2 นัด | พังเร็วพอให้ผู้เล่นแกะสลักแผนที่เอง (INV-6) |
| กำแพงเหล็ก | เจาะเกราะ 3 นัด | รางวัลของทีมที่ตอบถูกพร้อมกัน |

---

## 4. Data model

```ts
type Dir = 0|1|2|3|4|5|6|7   // 0=N, ทวนเข็ม 45° ต่อขั้น

type TileType = 'empty' | 'brick' | 'steel' | 'water' | 'bush'
// brick: กระสุนพัง (hp 2) · steel: เฉพาะ AP (hp 3) · water: กระสุนผ่าน รถผ่านไม่ได้
// bush: บังสายตา แต่วิ่งผ่านได้ (ของ Battle City — เก็บไว้ ทำให้ FOV มีความหมายขึ้น)

interface Tile { t: TileType; hp: number }

type AmmoKind = 'ap' | 'std' | 'dud'
// ap  : dmg 2   · พังอิฐ 2 ชั้น · พังเหล็กได้
// std : dmg 1   · พังอิฐ 1 ชั้น
// dud : dmg 0.5 · พังกำแพงไม่ได้ (ใช้หลอกล่อ)

interface Shell {
  kind: AmmoKind
  ownerId: string       // INV-3 — id คนที่ตอบถูกเร็วที่สุดของข้อนั้น
  ownerName: string
  questionId: string    // เอาไว้โชว์ใน kill cam
}

interface Tank {
  teamId: string
  x: number; y: number  // wu, มุมบนซ้าย
  hullDir: Dir          // 0,2,4,6 เท่านั้น (แกน)
  turretDir: Dir        // 8 ทิศ
  hp: number
  ammo: Shell[]         // สแต็ก — ยิงเอานัดบนสุด
  fireCdUntil: number
  dashCdUntil: number
  jammedUntil: number   // จาก consensus tier 'jam'
  alive: boolean
}

interface Bullet {
  x: number; y: number; dir: Dir
  shell: Shell
  teamId: string        // ห้ามยิงโดนตัวเอง
  bouncesLeft: number
}

interface Player {
  id: string; name: string
  teamId: string | null
  role: 'driver' | 'answerer' | 'ghost'
  pingsLeft: number
  pingCdUntil: number
  calledShotTurns: number   // ใช้ถ่วงน้ำหนักการสุ่มพลปืน (P5)
  stats: { correct: number; confidentCorrect: number; shellsOwned: number; kills: number }
}

interface Team {
  id: string; name: string; color: string; shape: 'A'|'B'|'C'|'D'  // ต้องต่างที่รูปทรงด้วย ไม่ใช่แค่สี
  tank: Tank
  driverId: string
  answererIds: string[]
  eliminatedAt: number | null
}

interface Question {
  id: string
  text: string
  choices: [string, string, string, string]
  correct: 0|1|2|3
  explain: string          // โชว์ให้ผีอ่าน (P7) และตอนจบแมตช์
  difficulty: 1|2|3        // map เข้ากับ round
  tags: string[]
  imageUrl?: string        // โจทย์ภาพ = กัน Google ได้ดีที่สุด
}

interface Submission {
  playerId: string
  choice: 0|1|2|3
  confident: boolean
  tMs: number              // ms นับจากคำถามขึ้น — ใช้หาเจ้าของกระสุน
}
```

---

## 5. อัลกอริทึม consensus (หัวใจของระบบ quiz)

**MUST:** ผลของคำถามไม่ใช่ ถูก/ผิด แต่เป็น **4 ระดับ** เพื่อบังคับให้คนตอบต้องคุยกันก่อนกด

```js
const WEIGHT = (correct, confident) =>
  correct ? (confident ?  2 : 1)
          : (confident ? -1 : 0);

function resolveQuestion(subs, question, teamAnswererCount) {
  if (subs.length === 0) return { tier: 'jam', W: 0, owner: null };

  const N = subs.length;                       // นับเฉพาะคนที่ส่งคำตอบ
  let score = 0;
  for (const s of subs) score += WEIGHT(s.choice === question.correct, s.confident);

  const W = Math.max(0, Math.min(1, score / (C.CONSENSUS_DIVISOR * N)));

  const tier = W >= 0.80 ? 'ap'
             : W >= 0.50 ? 'std'
             : W >= 0.20 ? 'dud'
             :             'jam';

  // INV-3 — เจ้าของกระสุน = คนตอบถูกที่เร็วที่สุด
  const owner = subs
    .filter(s => s.choice === question.correct)
    .sort((a, b) => a.tMs - b.tMs)[0] ?? null;

  return { tier, W, owner, participation: N / teamAnswererCount };
}
```

### ตารางผลลัพธ์

| W | Tier | ได้อะไร |
|---|---|---|
| ≥ 0.80 | `ap` | **กระสุนเจาะเกราะ** dmg 2 · ทะลุอิฐ 2 ชั้น · พังเหล็กได้ |
| 0.50–0.79 | `std` | **กระสุนมาตรฐาน** dmg 1 · พังอิฐ 1 ชั้น |
| 0.20–0.49 | `dud` | **กระสุนด้าน** dmg 0.5 · พังกำแพงไม่ได้ |
| < 0.20 | `jam` | **ปืนติดขัด 3 วิ** ไม่ได้กระสุน · `tank.jammedUntil = now + 3` |

### เคสตรวจสอบ (เขียน unit test ตามนี้)

| สถานการณ์ (N=10) | score | W | tier |
|---|---|---|---|
| ถูกหมด มั่นใจหมด | 20 | 1.00 | `ap` |
| ถูกหมด แต่ไม่มั่นใจหมด | 10 | 0.63 | `std` |
| 8 มั่นใจถูก + 2 ไม่มั่นใจถูก | 18 | 1.00 | `ap` |
| 5 มั่นใจถูก + 5 ไม่มั่นใจผิด | 10 | 0.63 | `std` |
| 5 มั่นใจถูก + 5 มั่นใจผิด | 5 | 0.31 | `dud` |
| 2 มั่นใจถูก + 8 มั่นใจผิด | −4 | 0.00 | `jam` |

> **ตั้งใจให้เป็นแบบนี้:** "ถูกหมดแต่ไม่มีใครกล้ามั่นใจ" ได้แค่กระสุนธรรมดา
> ความรู้อย่างเดียวไม่พอ ต้องกล้ายืนยันด้วย — นี่คือชั้นทักษะที่แยกเกมนี้ออกจาก Kahoot

### ยิงชี้เป้า (Called Shot — P5)

ทุกข้อที่ 3 (เริ่ม round 2) ให้ข้ามอัลกอริทึมข้างบน:

1. สุ่มคนตอบ 1 คนต่อทีม — **ถ่วงน้ำหนักผกผันกับ `calledShotTurns`** (คนที่ยังไม่เคยได้คิว โอกาสสูงกว่า) เพื่อให้ทุกคนได้เป็นฮีโร่อย่างน้อย 1 ครั้ง
2. คำตอบของเขาคนเดียวตัดสิน: ถูก → `ap`, ผิด → `jam`
3. ถ้าถูก เขาได้ **ลากเป้า** บนแผนที่ (ส่ง `{cx, cy}`) และนัดถัดไปของคนขับจะเลี้ยวเข้าหาเป้านั้น (homing อ่อน ๆ: ปรับทิศได้สูงสุด 45° ตอนออกจากลำกล้อง)
4. `calledShotTurns++`

---

## 6. Match state machine

```
LOBBY ──(host กด start, ≥2 ทีม)──> R1_EXPLORE
R1_EXPLORE ──(t≥120s)──> R2_CONTACT ──(t≥270s)──> R3_FRENZY ──(t≥360s หรือ เหลือ 2 ทีม)──> FINAL_DUEL
ทุก state ──(เหลือ 1 ทีม)──> KILLCAM ──> RESULTS
```

| State | Round idx | คำถาม | AMMO_CAP | เหตุการณ์ |
|---|---|---|---|---|
| `R1_EXPLORE` | 0 | ง่าย (diff 1) 12s | 6 | ทีมเกิดคนละมุม กำแพงหนา ยังไม่เจอกัน · **ยกนี้ทำหน้าที่สอนเกม อย่าใส่ tutorial แยก** |
| `R2_CONTACT` | 1 | กลาง (diff 2) 10s | 4 | storm ring #1 (inset 5) · ลังโบนัสสปอว์นกลางแผนที่ · เริ่ม called shot |
| `R3_FRENZY` | 2 | ยาก (diff 3) 7s ไม่มีช่องพัก | 3 | storm ring #2 (inset 8 → เหลือ 16×16 ≈ 1/4 ของแผนที่) · ผีเริ่มโยนลังเสบียง (§7) |
| `FINAL_DUEL` | 2 | **ข้อเดียวกันทั้ง 2 ทีม** | 3 | ทีมที่ W ≥ 0.50 **และเร็วกว่า** ได้ "นัดสังหาร" (dmg = ∞) 1 นัด ทีมช้ากว่าไม่ได้อะไร |

**MUST:** ตลอดทุก state วง game loop ของสนามรบไม่หยุด (INV-1) — คำถามเป็นเพียง overlay/ระบบขนาน

### Question cycle (วนตลอดตั้งแต่ R1)

```
QUESTION_OPEN (ANSWER_WINDOW วิ)  → รับ submission, ยิง tick ให้ client โชว์เวลาเหลือ
   ↓ (หมดเวลา — ไม่รอให้ครบทุกคน)
LOCK → resolveQuestion() ต่อทีม
   ↓
DELIVER → push shell เข้า tank.ammo (ถ้าเกิน AMMO_CAP ทิ้งนัดล่างสุด) · ส่งเฉลย+explain ลงมือถือรายคน (INV-5)
   ↓ (GAP_BETWEEN_Q = 3s, สนามยังเดิน)
QUESTION_OPEN (ข้อถัดไป)
```

---

## 7. ผี / คลังกลาง (P7)

เมื่อทีมถูกกำจัด สมาชิกทุกคน → `role = 'ghost'` **ห้ามเตะออกจากห้อง**

- ผีทุกทีมรวมเป็น **pool เดียว** ตอบคำถามชุดเดียวกับที่ทีมเป็นกำลังตอบ
- ใช้ `resolveQuestion` ตัวเดิม ถ้า `tier === 'ap' || tier === 'std'` → **สปอว์นลังเสบียงกลาง** ที่ cell ว่างสุ่มในวงปัจจุบัน (ทีมไหนขับไปชนก่อนได้ `ap` 1 นัด)
- ผีเห็น **เฉลย + `explain`** ของทุกข้อที่ตัวเองตอบผิด — เวลาที่ตายคือเวลาที่คนพร้อมเรียนที่สุด
- แสดงบนจอใหญ่: `"ผี 7 คนส่งเสบียงลงสนาม"` — ให้ผีรู้ว่าตัวเองยังมีตัวตน

---

## 8. Network protocol

ทุก message เป็น JSON `{ "t": "<type>", ... }`

### Phone → Server

| type | payload | หมายเหตุ |
|---|---|---|
| `join` | `{room, name}` | server ตอบ `joined` พร้อม `playerId`, `teamId` |
| `answer` | `{qid, choice, confident}` | server บันทึก `tMs` เอง **ห้ามเชื่อเวลาจาก client** |
| `ping` | `{cx, cy}` | ตรวจ `pingsLeft` และ `pingCdUntil` ฝั่ง server |
| `target` | `{cx, cy}` | เฉพาะคนที่ชนะ called shot |

### Driver → Server

| type | payload |
|---|---|
| `input` | `{move: Dir\|null, turret: Dir, fire: bool, dash: bool}` ส่งทุก tick |

### Server → Host screen

| type | payload | ความถี่ |
|---|---|---|
| `snapshot` | `{tanks[], bullets[], crates[], storm, phase, tLeft}` | 20 Hz |
| `tiles_delta` | `{changes:[{i, t, hp}]}` | เมื่อกำแพงเปลี่ยนเท่านั้น |
| `question` | `{qid, text, choices, endsAt, isCalledShot, calledPlayerNames}` | ต่อข้อ |
| `resolve` | `{teamId, tier, W, participation, ownerName}` | ต่อข้อ ต่อทีม — **ห้ามส่งคำตอบรายคน** (INV-5) |
| `killfeed` | `{victimTeam, killerTeam, ownerName, questionText}` | ต่อการตาย |
| `killcam` | `{frames[]}` | ตอนจบ |

### Server → Phone (รายคน)

| type | payload |
|---|---|
| `feedback` | `{correct, correctChoice, explain, weight, wasOwner}` — **ส่งเฉพาะเจ้าตัว** |
| `minimap` | `{tiles, tanks, storm}` 5 Hz — **คนตอบเห็นทั้งแผนที่** (INV-2) |
| `called_shot` | `{qid}` — คุณคือพลปืนข้อนี้ |

---

## 9. Juice checklist (ทำใน M2 ไม่ใช่ทำตอนท้าย)

ส่วนนี้ให้ผลต่อ "ความสนุก" มากกว่าการเพิ่มเมคานิกใหม่ และถูกกว่ามาก

- [ ] **Hit stop** — freeze 2 เฟรมตอนกระสุนโดนอะไรก็ตาม (คุ้มที่สุดต่อบรรทัดโค้ด)
- [ ] **Screen shake** — ยิง 3px/120ms · โดนยิง 8px/250ms · เกินนี้คนเมา
- [ ] **Brick debris** — อิฐพัง = เศษ 6 ชิ้นกระเด็นตามแรงโน้มถ่วง
- [ ] **Muzzle flash** 2 เฟรม + ปลอกกระสุนดีดออก
- [ ] **Ammo as object** — วาดปลอกกระสุนเรียงข้างป้อมปืน **ห้ามโชว์เป็นตัวเลขเฉย ๆ** ความขาดแคลนต้องมองเห็นได้ (INV-4)
- [ ] **Consensus chord** — เสียง "ตอบถูก" ซ้อนกันทีละคน ยิ่งคนตอบถูกเยอะยิ่งเป็นคอร์ดใหญ่ · ห้องจะได้ยินความเห็นตรงกันของตัวเอง
- [ ] **Timer pressure** — 3 วิสุดท้ายของหน้าต่างตอบ ขอบจอเต้น + เมโทรนอมเร่ง
- [ ] **Kill cam** 1.5 วิ พร้อมชื่อเจ้าของกระสุนและคำถามที่ทำให้ชนะ (INV-3)
- [ ] **Readability** — ทีมต่างกันที่ **รูปทรงเงา** ด้วย ไม่ใช่แค่สี · ต้องดูออกจากหลังห้อง · ต้องเล่นได้ถ้าตาบอดสี
- [ ] `imageSmoothingEnabled = false` + scale เป็นจำนวนเต็มเท่านั้น (×2/×3) ห้าม scale เศษส่วน

---

## 10. กติกาสนาม (รายละเอียดที่ต้อง implement ให้ตรง)

**การเคลื่อนที่ (ฟีล Battle City)**
- ตัวถังเดินได้ 4 แกนเท่านั้น (N/E/S/W) แกนตั้งฉาก snap ทุก `SNAP` (8 wu) → มุมกำแพงไม่ติด
- ป้อมปืนหมุนได้ 8 ทิศอิสระจากตัวถัง (**นี่คือส่วนที่เพิ่มเพดานฝีมือคนขับ ทำให้ตำแหน่งคนขับน่าอยากเป็น**)
- ห้ามพังกำแพงด้วยการชน ต้องใช้กระสุนเสมอ (บังคับให้ต้องพึ่งคนตอบ)

**กระสุน**
- `ap`/`std` ทำลาย tile ตามตาราง §4 · `dud` ไม่ทำลายอะไรเลย
- สะท้อนกำแพง **เหล็ก** ได้ 1 ครั้ง (มุมตกกระทบ = มุมสะท้อน) — เปิดทางให้ยิงมุมเทพ = คลิปที่คนอยากอัด
- กระสุนไม่ชนกระสุน (ทำให้ง่ายและอ่านง่ายกว่า)
- ยิงโดนทีมตัวเองไม่ได้

**Fog of war (INV-2)**
- คนขับ: raycast จากจุดกึ่งกลางรถถัง รัศมี `FOV_RADIUS` cells · `steel`/`brick`/`bush` บังสายตา
- นอกรัศมี: วาด tile ที่เคยเห็นแล้วเป็นสีจาง (memory) แต่ **ไม่โชว์รถถังศัตรู**
- คนตอบบนมือถือ: เห็นทุกอย่าง ไม่มี fog

**Storm**
- นอกวง: `-STORM_DPS` HP/s + กำแพงอิฐนอกวงถูกเผาทิ้ง (บังคับให้เจอกัน)
- วาดขอบวงเป็นเส้นเต้นสีแดง ให้เห็นชัดบนจอโปรเจกเตอร์

---

## 11. Milestones + acceptance criteria

**ห้ามข้ามลำดับ** ลำดับนี้ถูกออกแบบมาให้รู้เร็วที่สุดว่าเกมจะสนุกจริงไหม ก่อนลงแรงกับส่วนที่แพง

### M1 — สนามรบเปล่า
รถถัง 1 คัน เดินบนกริด ยิงได้ อิฐพังได้ ไม่มี quiz ไม่มีเน็ตเวิร์ก เล่นบนคอมเครื่องเดียว
✅ **ผ่านเมื่อ:** เดินชนกำแพงไม่ติดมุม · ยิงอิฐ 2 นัดแล้วพัง · 60 fps

### M2 — Juice ครบ
ทำทุกข้อใน §9 (ยกเว้น consensus chord และ kill cam)
✅ **ผ่านเมื่อ:** เช็คลิสต์ §9 ครบ · scale เป็นจำนวนเต็ม · ไม่มี blur

### M3 — 🚩 GATE: เอาให้คนสองคนยิงกันแล้วหัวเราะ
รถถัง 2 คันบนคีย์บอร์ดเดียวกัน ยิงกันเอง ไม่มี quiz
✅ **ผ่านเมื่อ:** ให้คนจริง 2 คนเล่น 5 นาทีแล้ว **เขาขอเล่นอีกรอบเอง**
❌ **ถ้าไม่ผ่าน: หยุด กลับไปแก้ §9 ระบบ quiz จะไม่ช่วยอะไรเลย** — อย่าเดินต่อ

### M4 — กระสุนจำกัด
`Tank.ammo` เป็นสแต็ก · `AMMO_CAP` · เติมด้วยลังบนแผนที่ไปก่อน (ยังไม่มีคำถาม)
✅ **ผ่านเมื่อ:** ผู้ทดสอบเริ่ม**ลังเลก่อนยิง** และเคยอยู่ในสภาพไม่มีกระสุน ≥3 ครั้ง/แมตช์ (INV-4)

### M5 — Server + มือถือ + consensus
WebSocket · room code · หน้ามือถือมีแค่ **4 ปุ่ม + 1 ธงมั่นใจ** · `resolveQuestion` + unit test ตามตาราง §5
✅ **ผ่านเมื่อ:** ทดสอบกับคนจริง 10 คน · latency กดถึงเห็นผล < 300ms · ไม่มีใครถามว่า "กดยังไง"

### M6 — INV-2 สายตาไม่เท่ากัน + ping
Fog of war ฝั่งคนขับ · minimap เต็มฝั่งมือถือ · ระบบ ping
✅ **ผ่านเมื่อ:** **วัดจากเสียงในห้อง** — ควรดังขึ้นชัดเจนเทียบกับ M5 · ถ้าห้องยังเงียบ แปลว่า fog ยังกว้างไป ลด `FOV_RADIUS`

### M7 — INV-3 ชื่อบนกระสุน
`Shell.ownerName` · kill feed · kill cam
✅ **ผ่านเมื่อ:** มีคนตะโกนชื่อตัวเองตอนเห็น kill feed (ถูกมาก ผลต่ออารมณ์สูงมาก)

### M8 — จังหวะแมตช์
State machine §6 · storm 2 วง · FINAL_DUEL
✅ **ผ่านเมื่อ:** 10 แมตช์ติดกันจบใน 6–8 นาทีทุกครั้ง · ไม่มีใครลุกออกกลางคัน

### M9 — สเกลถึง 30 คน
เดิมพันความมั่นใจ (มีตั้งแต่ M5 แล้ว) · called shot P5 · ผี P7
✅ **ผ่านเมื่อ:** ทดสอบ 30 คน · ทุกคนได้เป็นพลปืนอย่างน้อย 1 ครั้งใน 7 นาที · ไม่มีใครวางมือถือหลังตกรอบ

---

## 12. Question bank

`data/questions.json` — array ของ `Question` (schema §4)

**กัน Google:** เวลาสั้นช่วยได้ระดับหนึ่ง แต่ตัวช่วยจริงคือ**ชนิดของโจทย์**
ให้ bank มีอย่างน้อย 50% เป็นชนิดพวกนี้:

- โจทย์ **ภาพ** (`imageUrl`) — search ยากที่สุด
- โจทย์ **คำนวณ** ที่ต้องได้ตัวเลขจริง
- **"ข้อใด _ไม่ใช่_"** — negation ทำให้ copy-paste ไม่ได้คำตอบ
- **เรียงลำดับ** / จับคู่
- **สถานการณ์ประยุกต์** ("ถ้า X เกิด ควรทำอะไรก่อน")

**MUST:** สลับลำดับ choices แบบสุ่มต่อผู้เล่นแต่ละคน (ป้องกันการบอกกันว่า "กดข้อ C")

---

## 13. Non-goals ของ MVP

ห้ามเสียเวลากับสิ่งเหล่านี้จนกว่า M8 จะผ่าน:

- ระบบ account / โปรไฟล์ / ranking ข้ามแมตช์
- เล่นข้ามอินเทอร์เน็ต (MVP = LAN / ห้องเดียวกัน)
- AI bot
- ป้อมฐานที่ต้องป้องกันแบบ Battle City ต้นฉบับ — **ทับซ้อนกับกติกา "ทีมสุดท้ายที่รอด"** จะใส่ทั้งคู่ทำให้เกมสองใจ
  *ทางเลือกถ้าอยากได้ฐานจริง ๆ:* ให้ฐานเป็น **จุดเติมกระสุน** แทน ทีมจึงต้องเลือกระหว่างอยู่ใกล้เสบียงกับอยู่รอด
- โหมดเนื้อเรื่อง / แคมเปญ
- โมเดล 3D หรือเอฟเฟกต์ชั้นสูง

---

## 14. ความเสี่ยงที่ agent ควรเฝ้า

| ความเสี่ยง | อาการที่จะเห็นตอน playtest | ตัวแก้ |
|---|---|---|
| คนตอบเป็นตู้กดกระสุน | 8 ใน 10 คนก้มมือถือ ไม่มีใครมองจอใหญ่ | ลด `FOV_RADIUS` · เพิ่มความสำคัญของ ping |
| โหวตเจือจาง | มีคนกดข้อเดิมทุกข้อโดยไม่อ่าน | เช็คว่า kill feed ประกาศชื่อจริงไหม · ลด `CALLED_SHOT_EVERY` เป็น 2 |
| ตกรอบแล้ววางมือถือ | ห้องเงียบครึ่งหนึ่งตอนนาทีที่ 4 | เพิ่มอัตราสปอว์นลังของผี · โชว์จำนวนผีบนจอใหญ่ |
| คนเก่งคนเดียวแบก | ทีมรอคนเดิมตะโกนคำตอบทุกข้อ | คละ `tags` ให้กว้างขึ้น อย่าผูกวิชาเดียว |
| คนขับยืนนิ่งตอนกระสุนหมด | รถถังจอดรอคำตอบ | ลด `DASH_CD` · ทวนว่า ram ทำงานจริง |
| แมตช์ยืดจนเบื่อ | หลังนาที 7 คนลุกไปเข้าห้องน้ำ | เร่ง `STORM_RINGS` ให้เร็วขึ้น 20 วิ |

> **อย่าถามผู้ทดสอบว่า "สนุกไหม"** เขาจะตอบว่าสนุกเสมอ ให้ดูอาการในคอลัมน์กลางแทน มันโกหกไม่ได้

---

## 15. คำถามที่ต้องเคลียร์กับเจ้าของโปรเจกต์ก่อนเริ่ม M5

1. คนขับใช้อะไรควบคุม — คีย์บอร์ดที่เครื่องโฮสต์ หรือ virtual stick บนมือถือ? (กระทบ latency budget อย่างมาก)
2. จำนวนทีมสูงสุดต่อแมตช์เท่าไร? (spec นี้ตั้งไว้ 4 ทีม — ถ้าเกิน 6 ต้องขยายแผนที่)
3. Question bank มาจากไหน — พิมพ์เอง / นำเข้า CSV / ต่อกับระบบของมหาลัย?
4. ต้องเก็บผลการเรียนรู้รายคนไว้ให้อาจารย์ดูหลังจบไหม? (ถ้าใช่ ต้องเพิ่ม DB ซึ่งอยู่ใน non-goals ตอนนี้)

---

*Spec v0.1 · ตัวเลขทุกตัวเป็นสมมติฐาน ตั้งไว้ให้แก้หลัง playtest ครั้งแรก · หลังเสร็จแต่ละ milestone ให้รายงานว่าข้อไหนใน §11 ผ่าน/ไม่ผ่าน พร้อมเหตุผล*
